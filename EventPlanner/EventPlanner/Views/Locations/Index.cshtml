@model IEnumerable<EventPlanner.Data.Models.Location>
@{
    ViewData["Title"] = "Locations";
}

<h1>Locations</h1>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

@if (User.Identity?.IsAuthenticated == true)
{
    <a class="btn btn-success mb-3" asp-action="Create">Create Location</a>
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>City</th>
            <th>Name</th>
            <th>Address</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var l in Model)
    {
        <tr>
            <td>@l.City</td>
            <td>@l.Name</td>
            <td>@(l.Address ?? "-")</td>
            <td class="text-end">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@l.Id">Edit</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@l.Id">Delete</a>
                }
            </td>
        </tr>
    }
    </tbody>
</table>
